import React from 'react';

const Header = (props) => {
    return (
        <div>
            <h1>Favorite authors</h1>
            <div>
                {/* <button onClick={() => navigate("/player")}>List All Players</button>
                <button onClick={() => navigate("/player/new")}>Add Player</button> */}
                {/* <button onClick={() => navigate("/player/:_id")}>Edit A Player</button>
                <button onClick={() => navigate("/player/:_id")}>View a Player</button> */}
            </div>
        </div>
    )
};

export default Header;